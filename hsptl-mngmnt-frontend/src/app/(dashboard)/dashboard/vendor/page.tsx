import useToken from "@/hooks/use-token";
import Dashboard from "@/app/(dashboard)/components/root/Dashboard";
import Vendor from "../../components/root/Vendor";

const page = () => {
    return <Vendor />;
};

export default page;
